{
  "name": "websockets-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "format": "prettier --write ."
  },
  "dependencies": {
    "socket.io": "4.6.1",
    "socket.io-client": "4.6.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "prettier": "2.8.4",
    "typescript": "beta"
  }
}
